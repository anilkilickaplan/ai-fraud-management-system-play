# Copy to .env and fill in values (Dashboard → Settings → API).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=
# Optional: server-only; bypasses RLS. Never expose to clients.
# SUPABASE_SERVICE_ROLE_KEY=
